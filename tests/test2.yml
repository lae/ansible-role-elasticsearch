---
- hosts: test2.lxc
  remote_user: root
  become: True
  roles:
    - { role: williamyeh.oracle-java, tags: dependency }
    - ansible-role-elasticsearch
  vars:
    elasticsearch_cluster_name: test02
    elasticsearch_major_version: 5
    elasticsearch_minor_version: 0
  tasks:
    - debug: msg="THE FOLLOWING RUN IS FOR ELASTICSEARCH 5.X"

---
- hosts: test.lxc
  remote_user: root
  become: True
  roles:
    - { role: williamyeh.oracle-java, tags: dependency }
    - ansible-role-elasticsearch
  vars:
    elasticsearch_cluster_name: test01
  tasks:
    - debug: msg="THE FOLLOWING RUN IS FOR ELASTICSEARCH 2.X (default)"

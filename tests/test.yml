---
- hosts: test.lxc
  remote_user: root
  become: True
  vars:
    elasticsearch_cluster_name: test01
  tasks:
    - debug: msg="THIS RUN IS FOR ELASTICSEARCH 2.X (default)"
  roles:
    - { role: williamyeh.oracle-java, tags: dependency }
    - ansible-role-elasticsearch

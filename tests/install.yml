---
- hosts: localhost
  connection: local
  roles:
    - { name: lae.travis-lxc }
  vars:
    host_quantity: 3
    template: "{{ lookup('env', 'LXC_DISTRO') }}"
    release: "{{ lookup('env', 'LXC_RELEASE') }}"
